# Local default (safe to keep as-is):
DATABASE_URL=sqlite:///wedding_planner.db
# Supabase recommended (persistent): DATABASE_URL=<your-supabase-connection-string>
# Render SQLite (needs persistent disk mounted at /var/data): DATABASE_URL=sqlite:////var/data/wedding_planner.db
FRONTEND_ORIGIN=http://localhost:3000
